{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "The face host checks itself. The repository-root tsconfig covers `packages/*/src` and `scripts` — not `apps/*` — so without this the one function standing between a card page and a grey rectangle would be type checked by nothing.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    // Netlify Functions v2 runs on the platform's Node runtime, but the tests
    // beside them run under Bun. `@types/bun` is the root workspace's
    // devDependency and is hoisted, so naming it costs no manifest change and
    // no lockfile change — which matters, because CI installs frozen.
    "types": ["bun"]
  },
  "include": ["netlify/**/*.ts"],
  "exclude": ["node_modules", ".netlify"]
}
