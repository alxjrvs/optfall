/**
 * Compliance constants for the Optfall site.
 *
 * `docs/COMPLIANCE.md` ("The verbatim disclaimer") requires exactly one
 * exported constant that every surface rendering the disclaimer imports, so
 * that there is exactly one place a typo can live. This is that constant.
 *
 * Do not reword it, reflow it, shorten it, or let an editor autocorrect the
 * `®` or the `™`. The only permitted variation from Legend Story Studios'
 * template is the substitution of the application name — already made.
 *
 * Reproduced character for character from `docs/DISCLAIMER.md` ("Required
 * disclaimer") and `docs/COMPLIANCE.md` (the canonical text block). It is a
 * single source line on purpose: a literal grep of this file, of any component
 * that inlines it, and of the built HTML must all match the same byte string.
 */
export const LSS_DISCLAIMER =
  "Optfall is in no way affiliated with Legend Story Studios. Legend Story Studios®, Flesh and Blood™, and set names are trademarks of Legend Story Studios. Flesh and Blood characters, cards, logos, and art are property of Legend Story Studios.";
