# Generated by `bun run design-system`, and asserted byte-identical by
# `scripts/design-system-coverage.test.ts`. Collapsed by default in GitHub
# diffs because a ten-file component change arrives as a 33-file pull request,
# 23 of which are machine output — a review tax paid on every token or
# primitive change. This changes nothing about the enforcement: the files are
# still committed, still diffed, still required to match the generator.
design-system/** linguist-generated=true
